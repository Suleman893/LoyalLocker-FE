import { Button } from "@mui/material";
import React from "react";

const RoundedButton = () => {
  return (
    <Button style={{ width: "62px", height: "62px" }}>
      <img src="./images/btn.png" alt="" />
    </Button>
  );
};

export default RoundedButton;
